var expect = require('chai').expect;
var sinon = require('sinon');
var Undo = require('../problems/undo');

describe('Undo', function () {

    it('should apply an action to a context', function (done) {
        done();
    });

    it('should maintain order of operations', function (done) {
        done();
    });

    it('should allow you to revert to a specific time index', function (done) {
        done();
    });

    it('should allow an action to be rolled back', function (done) {
        done();
    });

    // extra credit! What else could we test here?
});