var expect = require('chai').expect;
var sinon = require('sinon');
var Queue = require('../problems/queue');

describe('Queue', function () {

    describe('Operations', function () {
        it('should maintain a series of functions', function (done) {
            done();
        });

        it('should allow you to add a function to the queue', function (done) {
            done();
        });

        it('should ensure that removed functions are elimiated from the queue', function (done) {
            done();
        });

        it('should execute a function every 1000 milliseconds while items are queued', function (done) {
            done();
        });
    });

    describe('Bookeeping', function () {

    });

    

    // extra credit! What else could we test here?
});